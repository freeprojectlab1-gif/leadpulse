{
  "name": "pro-emailer-saas",
  "version": "1.0.0",
  "description": "Professional Bulk Emailer SaaS",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && node server.js",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "npm run install-all && cd frontend && npm run build",
    "postinstall": "cd backend && rm -rf .cache/puppeteer && npx puppeteer browsers install chrome"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Muntazir Pro",
  "license": "ISC"
}
